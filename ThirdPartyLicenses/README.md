# Third-party license texts

Add the complete upstream license text for every Go module as a separate file
in this directory. Reference that path from the module's entry in
`DEPENDENCIES.json` and add any required attribution to the repository's
`NOTICE` file.

CI-only GitHub Actions are inventoried but not redistributed, so their license
texts do not need to be copied here.
